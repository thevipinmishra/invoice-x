import * as React from 'react';
import type { HTMLProps } from "../../utils/types.js";
export declare function usePopoverTitle(params: usePopoverTitle.Parameters): usePopoverTitle.ReturnValue;
export declare namespace usePopoverTitle {
  interface Parameters {
    titleId: string | undefined;
    setTitleId: React.Dispatch<React.SetStateAction<string | undefined>>;
  }
  interface ReturnValue {
    getTitleProps: (externalProps?: HTMLProps) => HTMLProps;
  }
}