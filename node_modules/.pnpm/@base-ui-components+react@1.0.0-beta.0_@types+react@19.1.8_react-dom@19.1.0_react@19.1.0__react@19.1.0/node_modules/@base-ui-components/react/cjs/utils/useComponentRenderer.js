"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useComponentRenderer = useComponentRenderer;
var _useRenderElement = require("./useRenderElement");
/**
 * Returns a function that renders a Base UI component.
 * @deprecated use `useRenderElement` instead.
 */
function useComponentRenderer(params) {
  const renderString = typeof params.render === 'string' ? params.render : undefined;
  const renderProp = typeof params.render === 'string' ? undefined : params.render;
  const renderElement = (0, _useRenderElement.useRenderElementLazy)(renderString, {
    className: params.className,
    render: renderProp
  }, {
    ...params,
    props: params.extraProps
  });
  return {
    renderElement
  };
}