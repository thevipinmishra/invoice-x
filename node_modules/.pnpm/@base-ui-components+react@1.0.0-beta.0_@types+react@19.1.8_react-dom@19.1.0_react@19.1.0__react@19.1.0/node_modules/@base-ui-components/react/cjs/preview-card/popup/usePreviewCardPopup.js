"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.usePreviewCardPopup = usePreviewCardPopup;
var React = _interopRequireWildcard(require("react"));
var _mergeProps = require("../../merge-props");
function usePreviewCardPopup(params) {
  const {
    getProps
  } = params;
  const getPopupProps = React.useCallback((externalProps = {}) => {
    return (0, _mergeProps.mergeProps)({
      style: {
        // <Popover.Arrow> must be relative to the <Popover.Popup> element.
        position: 'relative'
      }
    }, getProps(externalProps));
  }, [getProps]);
  return React.useMemo(() => ({
    getPopupProps
  }), [getPopupProps]);
}