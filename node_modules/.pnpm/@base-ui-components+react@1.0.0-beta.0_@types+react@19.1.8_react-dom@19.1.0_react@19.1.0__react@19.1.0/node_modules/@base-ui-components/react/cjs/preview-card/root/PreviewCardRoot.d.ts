import * as React from 'react';
import { usePreviewCardRoot } from "./usePreviewCardRoot.js";
import type { BaseOpenChangeReason } from "../../utils/translateOpenChangeReason.js";
/**
 * Groups all parts of the preview card.
 * Doesn’t render its own HTML element.
 *
 * Documentation: [Base UI Preview Card](https://base-ui.com/react/components/preview-card)
 */
export declare const PreviewCardRoot: React.FC<PreviewCardRoot.Props>;
export declare namespace PreviewCardRoot {
  interface State {}
  interface Props extends usePreviewCardRoot.Parameters {
    children?: React.ReactNode;
  }
  type Actions = usePreviewCardRoot.Actions;
  type OpenChangeReason = BaseOpenChangeReason;
}