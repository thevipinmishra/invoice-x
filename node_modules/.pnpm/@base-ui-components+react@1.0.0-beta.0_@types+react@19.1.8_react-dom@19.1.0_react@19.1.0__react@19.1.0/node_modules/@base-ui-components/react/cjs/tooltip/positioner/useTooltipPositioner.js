"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useTooltipPositioner = useTooltipPositioner;
var React = _interopRequireWildcard(require("react"));
var _useAnchorPositioning = require("../../utils/useAnchorPositioning");
var _TooltipRootContext = require("../root/TooltipRootContext");
function useTooltipPositioner(params) {
  const {
    open,
    trackCursorAxis,
    hoverable,
    mounted
  } = (0, _TooltipRootContext.useTooltipRootContext)();
  const positioning = (0, _useAnchorPositioning.useAnchorPositioning)(params);
  const props = React.useMemo(() => {
    const hiddenStyles = {};
    if (!open || trackCursorAxis === 'both' || !hoverable) {
      hiddenStyles.pointerEvents = 'none';
    }
    return {
      role: 'presentation',
      hidden: !mounted,
      style: {
        ...positioning.positionerStyles,
        ...hiddenStyles
      }
    };
  }, [open, trackCursorAxis, hoverable, mounted, positioning.positionerStyles]);
  return React.useMemo(() => ({
    props,
    ...positioning
  }), [props, positioning]);
}