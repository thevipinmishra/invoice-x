import { Side, useAnchorPositioning } from "../../utils/useAnchorPositioning.js";
import type { HTMLProps } from "../../utils/types.js";
export declare function useTooltipPositioner(params: useTooltipPositioner.Parameters): useTooltipPositioner.ReturnValue;
export declare namespace useTooltipPositioner {
  interface Parameters extends useAnchorPositioning.Parameters {}
  interface SharedParameters extends useAnchorPositioning.SharedParameters {
    /**
     * Which side of the anchor element to align the popup against.
     * May automatically change to avoid collisions.
     * @default 'top'
     */
    side?: Side;
  }
  interface ReturnValue extends useAnchorPositioning.ReturnValue {
    props: HTMLProps;
  }
}