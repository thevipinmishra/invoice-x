"use strict";
'use client';

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.AlertDialogTrigger = void 0;
var React = _interopRequireWildcard(require("react"));
var _AlertDialogRootContext = require("../root/AlertDialogRootContext");
var _useButton = require("../../use-button/useButton");
var _useComponentRenderer = require("../../utils/useComponentRenderer");
var _useForkRef = require("../../utils/useForkRef");
var _popupStateMapping = require("../../utils/popupStateMapping");
/**
 * A button that opens the alert dialog.
 * Renders a `<button>` element.
 *
 * Documentation: [Base UI Alert Dialog](https://base-ui.com/react/components/alert-dialog)
 */
const AlertDialogTrigger = exports.AlertDialogTrigger = /*#__PURE__*/React.forwardRef(function AlertDialogTrigger(props, forwardedRef) {
  const {
    render,
    className,
    disabled = false,
    ...other
  } = props;
  const {
    open,
    setTriggerElement,
    getTriggerProps
  } = (0, _AlertDialogRootContext.useAlertDialogRootContext)();
  const state = React.useMemo(() => ({
    disabled,
    open
  }), [disabled, open]);
  const mergedRef = (0, _useForkRef.useForkRef)(forwardedRef, setTriggerElement);
  const {
    getButtonProps
  } = (0, _useButton.useButton)({
    disabled,
    buttonRef: mergedRef
  });
  const {
    renderElement
  } = (0, _useComponentRenderer.useComponentRenderer)({
    render: render ?? 'button',
    className,
    state,
    propGetter: externalProps => getButtonProps(getTriggerProps(externalProps)),
    extraProps: other,
    customStyleHookMapping: _popupStateMapping.triggerOpenStateMapping,
    ref: mergedRef
  });
  return renderElement();
});
if (process.env.NODE_ENV !== "production") AlertDialogTrigger.displayName = "AlertDialogTrigger";