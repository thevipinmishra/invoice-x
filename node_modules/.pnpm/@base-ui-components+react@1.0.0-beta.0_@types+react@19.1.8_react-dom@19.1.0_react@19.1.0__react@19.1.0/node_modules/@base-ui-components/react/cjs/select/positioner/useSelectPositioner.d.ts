import type { HTMLProps } from "../../utils/types.js";
import { Side, useAnchorPositioning } from "../../utils/useAnchorPositioning.js";
export declare function useSelectPositioner(params: useSelectPositioner.Parameters): useSelectPositioner.ReturnValue;
export declare namespace useSelectPositioner {
  interface Parameters extends useAnchorPositioning.Parameters {
    alignItemWithTriggerActive: boolean;
  }
  interface SharedParameters extends useAnchorPositioning.SharedParameters {
    /**
     * Whether the positioner overlaps the trigger so the selected item's text is aligned with the trigger's value text. This only applies to mouse input and is automatically disabled if there is not enough space.
     * @default true
     */
    alignItemWithTrigger?: boolean;
  }
  interface ReturnValue extends Omit<useAnchorPositioning.ReturnValue, 'side'> {
    getPositionerProps: (externalProps?: HTMLProps) => HTMLProps;
    side: Side | 'none';
  }
}