"use strict";
'use client';

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.DialogClose = void 0;
var React = _interopRequireWildcard(require("react"));
var _useDialogClose = require("./useDialogClose");
var _DialogRootContext = require("../root/DialogRootContext");
var _useComponentRenderer = require("../../utils/useComponentRenderer");
/**
 * A button that closes the dialog.
 * Renders a `<button>` element.
 *
 * Documentation: [Base UI Dialog](https://base-ui.com/react/components/dialog)
 */
const DialogClose = exports.DialogClose = /*#__PURE__*/React.forwardRef(function DialogClose(props, forwardedRef) {
  const {
    render,
    className,
    disabled = false,
    ...other
  } = props;
  const {
    open,
    setOpen
  } = (0, _DialogRootContext.useDialogRootContext)();
  const {
    getRootProps
  } = (0, _useDialogClose.useDialogClose)({
    disabled,
    open,
    setOpen,
    rootRef: forwardedRef
  });
  const state = React.useMemo(() => ({
    disabled
  }), [disabled]);
  const {
    renderElement
  } = (0, _useComponentRenderer.useComponentRenderer)({
    render: render ?? 'button',
    className,
    state,
    propGetter: getRootProps,
    extraProps: other
  });
  return renderElement();
});
if (process.env.NODE_ENV !== "production") DialogClose.displayName = "DialogClose";