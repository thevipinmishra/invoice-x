import * as React from 'react';
import { HTMLProps, Orientation } from "../../utils/types.js";
import type { CompositeMetadata } from "../../composite/list/CompositeList.js";
import type { ToolbarItemMetadata } from "./ToolbarRoot.js";
export declare function useToolbarRoot(parameters: useToolbarRoot.Parameters): useToolbarRoot.ReturnValue;
export declare namespace useToolbarRoot {
  interface Parameters {
    disabled: boolean;
    /**
     * The component orientation.
     * @default 'horizontal'
     */
    orientation: Orientation;
  }
  interface ReturnValue {
    /**
     * Resolver for the Toolbar component's props.
     * @param externalProps additional props for Toolbar.Root
     * @returns props that should be spread on Toolbar.Root
     */
    getRootProps: (externalProps?: HTMLProps) => HTMLProps;
    disabledIndices: number[];
    setItemMap: React.Dispatch<React.SetStateAction<Map<Node, CompositeMetadata<ToolbarItemMetadata> | null>>>;
  }
}