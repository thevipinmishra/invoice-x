import * as React from 'react';
import { Dimensions } from "../collapsible/root/useCollapsibleRoot.js";
/**
 * Ensures the panel will expand to the correct height when the window is resized.
 * This prevents content from being cut off or the panel not fitting to the content.
 */
export declare function usePanelResize(panelRef: React.RefObject<HTMLElement | null>, setDimensions: React.Dispatch<React.SetStateAction<Dimensions>>, open: boolean): void;