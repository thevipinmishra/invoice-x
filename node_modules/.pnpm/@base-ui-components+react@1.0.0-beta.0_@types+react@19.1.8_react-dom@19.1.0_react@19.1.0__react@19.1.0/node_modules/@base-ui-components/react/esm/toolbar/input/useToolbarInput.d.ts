import * as React from 'react';
import { HTMLProps } from "../../utils/types.js";
export declare function useToolbarInput(parameters: useToolbarInput.Parameters): useToolbarInput.ReturnValue;
export declare namespace useToolbarInput {
  interface Parameters {
    /**
     * When `true` the item is disabled.
     */
    disabled: boolean;
    /**
     * When `true` the item remains focuseable when disabled.
     */
    focusableWhenDisabled: boolean;
    /**
     * The element ref.
     */
    ref?: React.Ref<Element>;
  }
  interface ReturnValue {
    getInputProps: (externalProps?: HTMLProps) => HTMLProps;
  }
}