'use client';

import * as React from 'react';
import { useDialogClose } from "./useDialogClose.js";
import { useDialogRootContext } from "../root/DialogRootContext.js";
import { useComponentRenderer } from "../../utils/useComponentRenderer.js";
/**
 * A button that closes the dialog.
 * Renders a `<button>` element.
 *
 * Documentation: [Base UI Dialog](https://base-ui.com/react/components/dialog)
 */
export const DialogClose = /*#__PURE__*/React.forwardRef(function DialogClose(props, forwardedRef) {
  const {
    render,
    className,
    disabled = false,
    ...other
  } = props;
  const {
    open,
    setOpen
  } = useDialogRootContext();
  const {
    getRootProps
  } = useDialogClose({
    disabled,
    open,
    setOpen,
    rootRef: forwardedRef
  });
  const state = React.useMemo(() => ({
    disabled
  }), [disabled]);
  const {
    renderElement
  } = useComponentRenderer({
    render: render ?? 'button',
    className,
    state,
    propGetter: getRootProps,
    extraProps: other
  });
  return renderElement();
});
if (process.env.NODE_ENV !== "production") DialogClose.displayName = "DialogClose";