'use client';

import * as React from 'react';
import { useAlertDialogRootContext } from "../root/AlertDialogRootContext.js";
import { useDialogClose } from "../../dialog/close/useDialogClose.js";
import { useComponentRenderer } from "../../utils/useComponentRenderer.js";
/**
 * A button that closes the alert dialog.
 * Renders a `<button>` element.
 *
 * Documentation: [Base UI Alert Dialog](https://base-ui.com/react/components/alert-dialog)
 */
export const AlertDialogClose = /*#__PURE__*/React.forwardRef(function AlertDialogClose(props, forwardedRef) {
  const {
    render,
    className,
    disabled = false,
    ...other
  } = props;
  const {
    open,
    setOpen
  } = useAlertDialogRootContext();
  const {
    getRootProps
  } = useDialogClose({
    disabled,
    open,
    setOpen,
    rootRef: forwardedRef
  });
  const state = React.useMemo(() => ({
    disabled
  }), [disabled]);
  const {
    renderElement
  } = useComponentRenderer({
    render: render ?? 'button',
    className,
    state,
    propGetter: getRootProps,
    extraProps: other
  });
  return renderElement();
});
if (process.env.NODE_ENV !== "production") AlertDialogClose.displayName = "AlertDialogClose";